# PROJECT domain lexicon (pt -> en) — tier-4 of the digest's match ladder.
# Entries here belong to THIS project: they extend AND override (per key) the
# generic business seed embedded in the scan tool, with no rebuild. Same shape
# as the seed: keys are the `pt` side, pre-folded to plain ASCII; values are
# the `en` synonyms (OR semantics, bidirectional lookup). A missing or invalid
# file is fine — the digest silently falls back to the embedded seed.

[terms]
# titulo = ["payable", "receivable"]
# apolice = ["policy"]
# sinistro = ["claim"]

---
name: {{sub}}-{{slug}}-pattern
description: "{{humanSuffix}} pattern detected in this project — {{fileCount}} files follow this convention. Use when adding a new {{humanSuffix}}, creating a class named *{{suffix}}, working in {{folderPattern}}{{folderKeywordsHint}}. Even if the user just says 'new {{humanSuffix}}' or 'add {{humanSuffix}}'."
source: scan
---
<!-- mustard:generated at:{{iso}} role:{{role}} -->

# {{humanSuffix}} Pattern

Detected convention: {{fileCount}} files matching `*{{suffix}}{{ext}}` across {{folderCount}} folder(s) in this project.

## Pattern

- Suffix: `{{suffix}}{{ext}}`
- Folder pattern: `{{folderPattern}}`
{{folderKeywordsSection}}{{#if commonBaseClass}}- Common base class: `{{commonBaseClass}}`{{/if}}
{{#if commonInterfaces}}- Common interfaces: `{{commonInterfaces}}`{{/if}}
- Naming: `{Something}{{suffix}}` (PascalCase prefix + suffix)

## Rules

- ALWAYS name new files `{Something}{{suffix}}{{ext}}`
- ALWAYS place in `{{folderPattern}}`
{{#if commonBaseClass}}- ALWAYS use `{{commonBaseClass}}` as base (or matching variant in same folder){{/if}}
- Mirror the structure of existing samples before diverging

## Samples in this project

{{#if samplesList}}
{{samplesList}}
{{/if}}

## References

For the most recent sample, read one of the files listed above before implementing.

name: CI

on:
  push:
    branches: [main, dev_rubens]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test (Linux)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: TypeScript build
        run: bun run build

      - name: Lint (zero warnings)
        run: bunx eslint src/ --max-warnings=0

      - name: Type-check strict
        run: bunx tsc --noEmit --strict -p tsconfig.json

      - name: Unit tests
        run: bun test tests/unit/

      - name: Integration tests
        run: bun test tests/integration/

      - name: Benchmarks (advisory)
        continue-on-error: true
        run: |
          bun run bench
          bun tests/bench/regression-check.cjs

  windows:
    name: Test (Windows — advisory)
    runs-on: windows-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: TypeScript build
        run: bun run build

      - name: EventStore tests (bun:sqlite on Windows)
        run: bun test tests/unit/event-store/
